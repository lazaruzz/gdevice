#include <stdio.h>
#include <gdevice.h>

int main()
{
    GD gdevice = gd_Create();
/*
    while(gd_IsOpen(gdevice)) 
    {
        // TODO gameloop
    }
*/

    printf("WalkerC: %i\n", window);
    return 0;
}
